Algoritmo "tabuada"

Var

  x, i, y : inteiro

Inicio

  escreva("Qual tabuada quer calcular? ")
  leia(x)
  
  para i de 1 ate 10 faca
       y <- x * i
       escreval(x, " x ", i, " = ", y)
  fimpara

Fimalgoritmo